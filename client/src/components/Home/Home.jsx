import React from "react";
import styles from './Home.module.css';
import { NavLink } from "react-router-dom";

const Home = () => {
    return (
        <div>
            NIY: acá van varios links y creo que cards
        </div>
    );
};

export default Home;